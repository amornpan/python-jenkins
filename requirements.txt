# Basic requirements for Python Calculator
# Keep it simple for Jenkins CI/CD

# No additional packages needed for basic functionality
# The app uses only Python standard library

# Optional: Add packages only if needed
# pytest==7.4.3
# black==23.10.1
# flake8==6.1.0
